@model IEnumerable<MyWebApplication8.User>

@{
    ViewBag.Title = "Home";
}

<h2>Home</h2>

<p>
    @Html.ActionLink("Create New", "AddNewUser")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.userid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.userid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                @Html.ActionLink("Edit", "UpdateUser", new { id = item.userid }) |
                @Html.ActionLink("Details", "UserDetails", new { id = item.userid }) |
                @Html.ActionLink("Delete", "DeleteUser", new { id = item.userid })
            </td>
        </tr>
    }
</table>


@*<h2>List of Countries from Viewbag</h2>
<ol>
    @foreach (string str in ViewBag.Countries)
    {
        <li>@str</li>
    }
</ol>

<h2>List of Countries from Dataview</h2>
<ol>
    @foreach (string str1 in ViewData["Countries1"] as List<string>)
    {
        <li>@str1</li>
    }
</ol>*@

@Html.ActionLink("Go to Webform", "Print");